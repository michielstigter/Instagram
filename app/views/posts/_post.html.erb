<h2><%= post.title %></h2>
<% if post.picture.present? %>
<%=link_to image_tag(post.picture.url(:thumb)), post.picture.url(:medium), :popup=>['original_image', 'height=700,width=900'],class: 'uploaded-pic' %>
<% end %>
<% post.tags.each do |tag| %>
  <li><%= link_to tag.text, tag %></li>
<% end %>